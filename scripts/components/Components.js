let input = (label, id) => `
    <div class="input_label-div" id="${id}">
        <label>${label}</label>
        <input type="text">
    </div>
`;

let select = (label, id) => `
    <div class="select_label-div" id="${id}">
        <label>${label}</label>
        <select></select>
    </div>
`;